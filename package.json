{
  "name": "gas",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@types/google-apps-script": "^1.0.16",
    "nodemon": "^2.0.4",
    "rimraf": "^3.0.2",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.2"
  },
  "scripts": {
    "dev": "NODE_ENV=local nodemon src/index.ts",
    "deploy": "rimraf ./dist && yarn tsc && clasp push"
  }
}
